
(place this in backend)
create or replace procedure cal is
cursor c1 is select sum(ptot_price) as sum from cart;
a number(5):=0;
begin
for q in c1
loop
a:=q.sum;
dbms_output.put_line(a);
end loop;
end;
/

(place this in java code)
set serveroutput on
begin
cal;
end;
/


the sum which we were calculating for cart..will be calculated by this now...try implementing it...
